#include "recherche.h"

#include <stdio.h>

void rechercher(int mode /*TODO*/) {
    switch(mode) {
        case MODE_RECH_TEXTE:
            printf("Blop\n");
            break;
        case MODE_RECH_IMAGE:
            break;
        case MODE_RECH_AUDIO:
            break;
        default:
            break;
    }
}
